{
  "name": "slack-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Slack-First AI Company Platform",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "docker:build": "docker compose build",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  }
}
